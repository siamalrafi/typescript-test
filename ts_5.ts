/* function logStringOrError(value: unknown) {
   if (typeof value === "string") {
      console.log(value);
   } else {
      console.error("The value is not a string");
   }
}
const myString = "Hello, world!";
const myNumber = 123;

logStringOrError(myString);
logStringOrError(myNumber);
 */

// another one ------------
/* function logString(input: unknown): void {
   if (typeof input === "string") {
      console.log(input);
   } else {
      console.error("Input is not a string.");
   }
}

// Test the function
logString("Hello, world!"); // Logs: Hello, world!
logString(123); // Logs: Input is not a string.
 */
